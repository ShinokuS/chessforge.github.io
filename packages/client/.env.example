# Firebase Realtime Database — онлайн без WebRTC (работает с VPN / Pages / Vercel)
# https://console.firebase.google.com/ → Create project → Build → Realtime Database → Create
# Rules (для хобби, на время теста):
# {
#   "rules": {
#     "rooms": {
#       ".read": true,
#       ".write": true
#     }
#   }
# }
# Project settings → Your apps → Web → скопируйте конфиг:

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
